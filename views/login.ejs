<%- include('partials/header') -%>

<main class="container">
  <div class="row justify-content-center">
    <section class="col-6 mt-5">
      <% if (locals.messages.errors) { %> <% messages.errors.forEach( el => { %>
      <div class="alert alert-danger"><%= el.msg %></div>
      <% }) %> <% } %> <% if (locals.messages.info) { %> <%
      messages.info.forEach( el => { %>
      <div class="alert alert-info"><%= el.msg %></div>
      <% }) %> <% } %>
      <form action="/login" method="POST">
        <div class="mb-3">
          <label for="exampleInputEmail1" class="form-label"
            >Email address</label
          >
          <input
            type="email"
            class="form-control"
            id="exampleInputEmail1"
            aria-describedby="emailHelp"
            name="email"
          />
        </div>
        <div class="mb-3">
          <label for="exampleInputPassword1" class="form-label">Password</label>
          <input
            type="password"
            class="form-control"
            id="exampleInputPassword1"
            name="password"
          />
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
      </form>
    </section>
  </div>
</main>

<%- include('partials/footer') -%>

<!--
  The code you provided is an EJS (Embedded JavaScript) template code for a login page. It includes partials for the header and footer and displays a login form.

Here's a breakdown of the code:

ejs

<%- include('partials/header') -%>

This line includes the contents of the partials/header.ejs file. The - character after the opening tag (<%-) and before the closing tag (-%>) removes any whitespace that may be generated during the inclusion.

ejs

<main class="container">
  <div class="row justify-content-center">
    <section class="col-6 mt-5">
      <% if (locals.messages.errors) { %> <% messages.errors.forEach( el => { %>
      <div class="alert alert-danger"><%= el.msg %></div>
      <% }) %> <% } %> <% if (locals.messages.info) { %> <%
      messages.info.forEach( el => { %>
      <div class="alert alert-info"><%= el.msg %></div>
      <% }) %> <% } %>
      <form action="/login" method="POST">
        <div class="mb-3">
          <label for="exampleInputEmail1" class="form-label"
            >Email address</label
          >
          <input
            type="email"
            class="form-control"
            id="exampleInputEmail1"
            aria-describedby="emailHelp"
            name="email"
          />
        </div>
        <div class="mb-3">
          <label for="exampleInputPassword1" class="form-label">Password</label>
          <input
            type="password"
            class="form-control"
            id="exampleInputPassword1"
            name="password"
          />
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
      </form>
    </section>
  </div>
</main>

This section represents the main content of the login page. It uses Bootstrap classes to create a responsive layout with a container. Inside the container, there is a row with justify-content-center class, which centers the login form horizontally. The form is contained within a section with col-6 class, occupying 6 columns in the row.

Inside the section, there are conditionals (<% if ... %>) to check if there are any error messages or info messages to display. If there are errors, it loops through the messages.errors array and displays each error message in a <div> with the alert-danger class. If there are info messages, it loops through the messages.info array and displays each info message in a <div> with the alert-info class.

Below the error and info messages, there is a login form. It has two input fields for email and password, along with labels and Bootstrap form control classes. The form has an action attribute set to /login and uses the POST method to submit the form.

Finally, there is a submit button with the btn-primary class to submit the form.

ejs

<%- include('partials/footer') -%>

This line includes the contents of the partials/footer.ejs file, similar to the header inclusion.

Overall, this code structure allows you to display a login form with error and info messages using Bootstrap classes and EJS templating.
-->
